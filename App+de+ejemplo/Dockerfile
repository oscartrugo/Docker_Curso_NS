FROM indicar la imagen de la base
WORKDIR - cambiar el directorio de trabajo
COPY source dest - Copiar los archivos del local al contenedor
ADD source dest
RUN command
ENV key=value - Variables de entorno
EXPOSE port - Exponer los puertos
USER daemon
CMD [ "executable" ]
ENTRYPOINT [ "executable" ]